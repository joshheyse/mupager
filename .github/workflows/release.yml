name: Release
on:
  push:
    tags: ['v*']
permissions:
  contents: write
jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            artifact: mupager-x86_64-linux
            install: >-
              sudo apt-get update && sudo apt-get install -y
              libmupdf-dev libncursesw5-dev libfreetype-dev
              libharfbuzz-dev libjpeg-dev libopenjp2-7-dev
              libgumbo-dev zlib1g-dev
            cmake_extra: ""
          - os: macos-latest
            artifact: mupager-aarch64-darwin
            install: brew install mupdf ncurses
            cmake_extra: ""
          - os: ubuntu-latest
            artifact: mupager-x86_64-linux-portable
            install: >-
              sudo apt-get update && sudo apt-get install -y
              libmupdf-dev libncursesw5-dev libfreetype-dev
              libharfbuzz-dev libjpeg-dev libopenjp2-7-dev
              libgumbo-dev zlib1g-dev
            cmake_extra: -DMUPAGER_STATIC_DEPS=ON
          - os: macos-latest
            artifact: mupager-aarch64-darwin-portable
            install: brew install mupdf ncurses
            cmake_extra: -DMUPAGER_STATIC_DEPS=ON
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install dependencies
        run: ${{ matrix.install }}
      - name: Set Homebrew pkg-config paths
        if: runner.os == 'macOS'
        run: |
          echo "PKG_CONFIG_PATH=$(brew --prefix mupdf)/lib/pkgconfig:$(brew --prefix ncurses)/lib/pkgconfig:${PKG_CONFIG_PATH:-}" >> "$GITHUB_ENV"
          echo "CMAKE_PREFIX_PATH=$(brew --prefix mupdf);$(brew --prefix ncurses)" >> "$GITHUB_ENV"
      - name: Configure
        run: cmake -DCMAKE_BUILD_TYPE=Release -DCMAKE_PREFIX_PATH="${CMAKE_PREFIX_PATH:-}" ${{ matrix.cmake_extra }} -S . -B build
      - name: Build
        run: cmake --build build --config Release -j$(nproc 2>/dev/null || sysctl -n hw.ncpu)
      - name: Copy binary
        run: cp build/src/mupager ${{ matrix.artifact }}
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}
  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
      - uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: artifacts/mupager-*/mupager-*
